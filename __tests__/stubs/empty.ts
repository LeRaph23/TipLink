// Vitest stub for the `server-only` / `client-only` packages.
// Next.js aliases these to build-time markers with no runtime behaviour;
// Vitest has no such alias, so source files importing them resolve here.
export {};
